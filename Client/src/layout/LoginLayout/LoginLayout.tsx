import React from "react";
import { LoginLayoutProps } from "../../types/types";


const LoginLayout: React.FC<LoginLayoutProps> = ({ children }) => {
  return <>{children}</>;
};

export default LoginLayout;
